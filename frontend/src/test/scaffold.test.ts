// Placeholder test — ensures the test runner works before any features are implemented.
// Remove this file once real tests are added.
import { describe, it, expect } from 'vitest';

describe('scaffold', () => {
  it('test environment is configured correctly', () => {
    expect(true).toBe(true);
  });
});
